Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. Deployment notes: The ML model uses FAISS for vector search. Embeddings are generated via OpenAI. Indexing pipeline runs nightly and stores metadata including doc_id and chunk_id. 